#!/bin/bash 

name="$1"

cd /var/lib/lxc

for app in app-*;
do
        actname=$(cat $app/appts.name)
        if [ "$name" = "$actname" ]; then
                echo "$app"
                exit 0
        fi
done
exit 1

